# RealSaS-OPT — Current State

**Date:** 2026-08-24  
**Active branch:** `audit/iris-architecture-discipline-20260824`  
**Draft PR:** `#4` — audit only, not merged  
**Status:** `R0_R3_EXECUTION_CI69_FROZEN__REAL_RUN_NEXT__OPTIMIZER_ZERO__TRAINING_FORBIDDEN`

## Read this first

This file is the single continuation authority.

The preserved pre-audit branch is `g0-g1/single-pose-geometry`. Controlled V1/M256 executables are historical/no-run. The sole active candidate implementation is `experiments/iris_single_pose_v2/`.

**NO optimizer run is authorized.** Architecture coding is no longer the blocker. The only next scientific executable is the real frozen 256-asset R0-R3 Representation Authority measurement, followed by a separate canonical interpretation.

## Final pre-result execution freeze

Authority:

`experiments/post_corpus_audit/REPRESENTATION_AUTHORITY_EXECUTION_FREEZE_CI69_20260824.md`

Exact execution source head:

`e110014e79f3a9686bfd411e41454a627914fe14`

Exact-head GitHub Actions authority:

- workflow: `IRIS V2 Preflight`
- run: **#69**
- run ID: `32672422405`
- conclusion: **SUCCESS**

Run #69 passed exact source compilation, architecture/coordinate/matcher preflight, physical firewall/cache preflight, observable evaluator preflight, Representation Authority semantic preflight, execution-bundle construction and artifact upload.

The final integrity amendment at `e110014...` binds `run_representation_authority_v1.py` itself into `RUN_AUTHORITY_V1.json`'s script-hash chain. Interrupted/resumed runs therefore fail closed if the orchestrator changes.

### Final execution bundle

GitHub artifact ID: `9501738915`

ZIP SHA-256:

`31b8bee0ebae9d91b45a58f41134da7d20d479aa3d855d9012f7837c84d4770f`

`BUNDLE_INFO.source_head_sha`:

`e110014e79f3a9686bfd411e41454a627914fe14`

All eight bundled execution scripts independently passed the internal `SHA256SUMS.txt` verification after download.

Drive mirror:

`RealSaS_MASTER_CORPUS_1024_V3/reports/iris_single_pose_v2/IRIS_V2_R0_R3_EXECUTION_BUNDLE_CI69.zip`

Drive file ID: `1lW7pmAjjGO-M1OWvkJn9vOphqhv29QNP`

Prepared Colab launcher:

`RealSaS_IRIS_V2_R0_R3_Representation_Authority_CI69.ipynb`

Notebook SHA-256:

`dd901089c31650cf5a69d79b15a594994717b1d7397e65fa4a91a7f0d41c8a13`

## Controlled corpus authority

- canonical selected: **3993**;
- excluded before this study: 56 repair-pending current-A-render mismatches, 6 active non-Basis shape-key assets, 1 all-eight-view blank asset;
- controlled corpus: **3930**;
- FIT: **2935 OPEN**;
- TUNE: **313 OPEN**;
- CAL: 246 SEALED;
- DEV: 270 SEALED;
- EXTERNAL_HOLDOUT: 166 SEALED;
- open FIT+TUNE: **3248**.

Frozen source bytes:

- `IRIS_CONTROLLED_V1_SPLIT_FREEZE.json` SHA-256 `9e766ac61126c9b4787eef24146e36aac40cbeac166d67ba898f8b79133e9d66`
- `CANONICAL_VARIANT_SELECTION.json` SHA-256 `af2436d2a25a6f715e2d81af14b731837c7b02b609f1a4fc206acb591beb61c9`

CAL/DEV/EXTERNAL remain sealed. No resplitting and no post-apparatus asset substitution.

## Frozen confirmatory panel

- **256 OPEN assets**;
- at most 48 queries/asset: 16 adjacent, 16 skip-one, 16 opposite;
- split: 230 FIT / 26 TUNE;
- provider: 245 Objaverse / 7 Quaternius / 4 KayKit;
- strongest capability: 217 Arachne / 33 Geppetto / 6 IRIS;
- 12 non-empty `(provider, split, strongest-capability)` strata;
- ordered asset-ID-list SHA-256: `366b5fffb1ff93c1c7bbad0ac4746c4f2675a633ec01745c026cecb2b7820961`.

## Frozen R0-R3 semantics

- SAME-locus truth = exact physical P within `0.003` canonical units;
- R0 = exact P;
- R1 = exact P+N;
- R2 P sigma = `0, .0005, .001, .0025, .005, .01`;
- R3 = full 6x5 P/N grid with N angles `0, 5, 10, 20, 40` degrees;
- P+N diagnostic score = `dP + 0.05*(1-cos N)`, fixed before results and never swept;
- both source and target observations independently perturbed;
- deterministic Philox noise per asset/arm/view/track;
- exact tangent-plane normal rotation;
- top1/top4/top8 = physical set-containment, not cached-index accuracy;
- reciprocal + three-view cycle use legal candidate universes;
- component buckets `1`, `2-4`, `5-16`, `17+`;
- support buckets `2-3`, `4-5`, `6-8`;
- family containment and physical-error median/p90/p95 distributions required;
- `nearest_non_equivalent_physical_gap` is geometry-only structural confusability, not semantic symmetry or a promotion threshold;
- mean-only promotion forbidden.

R4 remains intentionally undefined. If R0-R3 evidence motivates it, a separate formulation prereg must be frozen before any R4 result is opened.

## Canonical IRIS boundary

Shipping input remains `ONE neutral pose x 8 ordered views`.

IRIS owns observable geometric/correspondence evidence for a deterministic SurfaceBuilder. It does not own authored joint/owner identity, source-rig exact partition, hierarchy, skinning or mandatory GFDR.

```text
RGBA x 8
  -> shared high-resolution encoder
  -> local f16 reasoning
  -> fixed pooled multiview context + known yaw
  -> Z_coarse @ R/8: global high-recall basin admission
  -> P/N/U_geo @ R/2 + P rescue
  -> Z_fine @ R/2: LOCAL refinement only
  -> reciprocal/cycle SUPPORT
  -> set-valued hypotheses
  -> deterministic SurfaceBuilder
```

## Real execution

Preferred launcher: `RealSaS_IRIS_V2_R0_R3_Representation_Authority_CI69.ipynb` → **Run all**.

The notebook:

1. mounts Drive;
2. verifies the exact CI69 ZIP SHA and source-head authority;
3. verifies all internal script SHA values and orchestrator self-hash lock;
4. keeps large scratch staging/cache under `/content` to reduce Drive FUSE write pressure;
5. runs only `run_representation_authority_v1.py --mode all` with optimizer=0;
6. on recognized transient Drive I/O only, remounts and retries the same frozen run;
7. persists final evidence/manifests to:
   `RealSaS_MASTER_CORPUS_1024_V3/runs/IRIS_SINGLE_POSE_V2_REPRESENTATION_AUTHORITY_V1_CI69_RESULT`.

A successful measurement may emit only:

`R0_R3_MEASURED__CANONICAL_INTERPRETATION_REQUIRED`

with:

- `optimizer_steps = 0`;
- `training_authorized = false`;
- `sealed_splits_opened = false`.

The runner/notebook may not decide P sufficiency, R4, SOI-2 or training authorization.

## NEXT EXECUTABLE STEP

**Run the CI69 Colab notebook. Do not train.**

After the persistent result exists, inspect `REPRESENTATION_AUTHORITY_COMPACT_HANDOFF_V1.json` plus the full result/hard-tail and write one canonical interpretation without changing frozen definitions.

Only after that interpretation:

- if legal P/P+N closes the representation gate: freeze the learner target and run production-width optimizer=0 GPU capacity preflight;
- if a reproducible residual warrants R4: freeze a separate `R4_FORMULATION_PREREG` before R4 results;
- if legal representation remains insufficient: proceed to SOI-2/target-authority work;
- only after representation closure + GPU preflight + frozen mini membership/style/checkpoint key + new prereg may an optimizer run be considered.

## Freeze rule

Execution code is now frozen until the real result is opened. Only a documented genuine apparatus failure may reopen it; any fix requires a new exact-head CI/bundle/Drive mirror/launcher authority and may not be silently interpreted as a scientific result.

## Research rule

`apparatus/data -> representation/target -> learner/optimizer -> evidence consumer -> downstream sufficiency -> only then information limit`
