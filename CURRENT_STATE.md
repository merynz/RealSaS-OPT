# RealSaS-OPT — Current State

**Date:** 2026-08-24  
**Active branch:** `audit/iris-architecture-discipline-20260824`  
**Draft PR:** `#4` — audit only, not merged  
**Status:** `IRIS_V2_CODE_AND_REPRESENTATION_APPARATUS_PASS__REAL_R0_R3_RUN_NEXT__TRAINING_FORBIDDEN`

## Read this first

This file is the single continuation authority. The preserved pre-audit branch is `g0-g1/single-pose-geometry`.

**NO optimizer run is authorized.** CAL/DEV/EXTERNAL remain sealed for learner/model-selection use.

The immediate remaining scientific task is the real frozen **optimizer=0 R0-R3 Representation Authority measurement** and its subsequent canonical interpretation. Architecture coding is not the current blocker.

## Controlled-corpus authority

Later Stage-A/B3/B4/B5/B6 authority narrowed the older blanket Gate1–5 block.

Current controlled population:

- canonical selected: 3993;
- 56 repair-pending current-A-render mismatches excluded;
- 6 active non-Basis shape-key assets excluded;
- 1 all-eight-view blank asset excluded;
- controlled corpus: **3930**;
- FIT: **2935 OPEN**;
- TUNE: **313 OPEN**;
- CAL: 246 SEALED;
- DEV: 270 SEALED;
- EXTERNAL_HOLDOUT: 166 SEALED;
- FIT+TUNE open population: **3248**.

Gate-3 appearance recovery remains mandatory before product/natural-domain qualification. It does not block the preregistered controlled geometry-observation Representation Authority study.

SOI-2 is conditional on legal representation remaining insufficient and is mandatory before an impossibility/information-limit claim; it is not automatically required before every controlled learner run if the legal representation ceiling closes.

## Frozen source bytes

The active Representation gate refuses to start unless these exact source authorities match:

- `IRIS_CONTROLLED_V1_SPLIT_FREEZE.json` SHA-256  
  `9e766ac61126c9b4787eef24146e36aac40cbeac166d67ba898f8b79133e9d66`
- `CANONICAL_VARIANT_SELECTION.json` SHA-256  
  `af2436d2a25a6f715e2d81af14b731837c7b02b609f1a4fc206acb591beb61c9`

No membership is reconstructed heuristically.

## Canonical IRIS boundary

Shipping input remains:

`ONE neutral pose x 8 ordered views`

IRIS owns observable geometric/correspondence evidence for a deterministic SurfaceBuilder. It does **not** own authored joint/owner identity, source-rig exact partition, hierarchy, skinning or mandatory GFDR.

Current evidence family:

- P common/object-frame surface position;
- N geometric orientation;
- U_geo geometric predictive risk only;
- Z_coarse high-recall/persistence evidence;
- Z_fine local-only correspondence evidence;
- direct visibility/support;
- reciprocal/cycle support;
- provenance;
- set-valued ambiguity when singleton evidence is insufficient.

## Active V2 implementation

Only active candidate code:

`experiments/iris_single_pose_v2/`

```text
RGBA x 8
  -> shared high-resolution encoder
  -> full local f16 reasoning
  -> fixed pooled global multiview context + known yaw
  -> fuse context back into local field
  -> Z_coarse @ R/8: global high-recall basin admission
  -> P/N/U_geo @ R/2 + P rescue
  -> Z_fine @ R/2: LOCAL refinement only
  -> reciprocal/cycle SUPPORT
  -> set-valued hypotheses
  -> deterministic SurfaceBuilder
```

No fixed `max_w`; no fixed-128 native authority; no global Z_fine admission; no premature singleton.

Audit fixes now executable:

1. reciprocal training is legal-positive-set aware, not cached-ID diagonal-only;
2. accepted correspondence XY stays exact continuous projection; raster is a visibility/surface witness;
3. observation SupCon sampling is view-balanced rather than view-major prefix truncation;
4. stage/cache reuse is source/settings/builder-semantic fingerprinted;
5. physical IRIS staging retains only `vertices/faces` plus legal observation authority;
6. deterministic reciprocal/cycle is support evidence, never a silent hard-delete or singleton authority;
7. evaluator reports object-space tails, exact coarse cells and native-1024-pixel localization;
8. evaluator defines no checkpoint-selection key before preregistration;
9. Controlled V1/M256 executables are explicitly historical via `experiments/iris_controlled_v1/HISTORICAL_ONLY__DO_NOT_RUN.md`.

## Exact committed-code CI

Workflow: `.github/workflows/iris_v2_preflight.yml`

**Last verified code-bearing head:** GitHub Actions **run #51 / ID `32671113352` — SUCCESS**.

That run includes the current R0-R3 measurement code and passes:

- exact source compilation;
- architecture / coordinate / matcher preflight;
- physical firewall / stale-cache invalidation preflight;
- observable evaluator preflight;
- Representation Authority semantic preflight.

Representation semantic preflight verifies:

- 38 frozen R0-R3 arms;
- exact-angle N perturbation;
- deterministic Philox observation noise;
- physical set-valued truth with multiple cached indices inside one legal SAME set;
- reciprocal/cycle semantics;
- family median/p90/p95 tail output;
- geometry-only nearest-non-equivalent confusability diagnostic without claiming semantic symmetry;
- optimizer steps = 0.

Subsequent handoff/documentation commits may trigger their own CI, but they do not alter the V2 Python bytes verified by code-bearing run #51 unless explicitly noted here.

## Frozen Representation Authority apparatus

Parent prereg:

`experiments/post_corpus_audit/REPRESENTATION_AUTHORITY_STUDY_V1_PREREG.md`

Execution addendum:

`experiments/post_corpus_audit/REPRESENTATION_AUTHORITY_STUDY_V1_EXECUTION_ADDENDUM_20260824.md`

Exact panel lock:

`experiments/post_corpus_audit/REPRESENTATION_AUTHORITY_PANEL_LOCK_V1.json`

Runbook:

`experiments/iris_single_pose_v2/R0_R3_RUNBOOK_V1.md`

### Exact pre-result confirmatory panel

Frozen before any new R0-R3 result:

- **256 OPEN assets**;
- 48 queries/asset maximum: 16 adjacent, 16 skip-one, 16 opposite;
- split: 230 FIT / 26 TUNE;
- provider: 245 Objaverse / 7 Quaternius / 4 KayKit;
- strongest capability: 217 Arachne / 33 Geppetto / 6 IRIS;
- 12 non-empty `(provider, split, strongest-capability)` strata represented;
- ordered asset-ID-list SHA-256:  
  `366b5fffb1ff93c1c7bbad0ac4746c4f2675a633ec01745c026cecb2b7820961`.

No post-apparatus substitution is allowed. One locked asset failing legal staging/cache/audit invalidates the confirmatory run instead of selecting a replacement.

### Frozen R0-R3 semantics

- SAME-locus truth = exact physical P within `0.003` canonical units;
- R0 = exact P;
- R1 = exact P+N;
- R2 P-noise sigma = `0, .0005, .001, .0025, .005, .01`;
- R3 = full 6x5 cross-product with N angles `0, 5, 10, 20, 40` degrees;
- P+N diagnostic score = `dP + 0.05*(1-cos N)` fixed before results; no coefficient sweep;
- source and target observations independently perturbed;
- noise deterministic per asset/arm/view/track with Philox;
- N perturbation is an exact tangent-plane rotation;
- top1/top4/top8 are physical set-containment, not track-index accuracy;
- reciprocal and three-view cycle use legal candidate universes;
- connected-component buckets = `1`, `2-4`, `5-16`, `17+`;
- support buckets = `2-3`, `4-5`, `6-8`;
- family-level containment + physical-error median/p90/p95 distributions required;
- `nearest_non_equivalent_physical_gap` is a descriptive geometry-only structural-confusability quantity, **not** a semantic symmetry label or promotion threshold;
- mean-only promotion forbidden.

R4 remains intentionally undefined. If R0-R3 evidence justifies relational addressing, a separate `R4_FORMULATION_PREREG` must freeze its exact mathematics before any R4 result is opened.

## One-command optimizer-zero gate

Executable:

`experiments/iris_single_pose_v2/run_representation_authority_v1.py`

Default corpus root:

`/content/drive/MyDrive/RealSaS_MASTER_CORPUS_1024_V3`

Command:

```bash
python run_representation_authority_v1.py --mode all
```

It performs only:

```text
verify frozen source-authority SHA
  -> derive exact locked 256 seed
  -> verify pre-stage panel digest
  -> physical geometry-only staging
  -> exact/dense V2 truth cache
  -> read-only stage/cache audit
  -> 38 R0-R3 measurements
  -> verify identical post-result panel digest
  -> RUN_COMPLETE_V1.json
```

The completed legal measurement must retain:

- `optimizer_steps = 0`;
- `training_authorized = false`;
- `sealed_splits_opened = false`;
- status `R0_R3_MEASURED__CANONICAL_INTERPRETATION_REQUIRED`.

The measurement runner is forbidden from deciding P sufficiency, R4 necessity, SOI-2 or training authorization.

## NEXT EXECUTABLE STEP

**Execute the real frozen 256-asset R0-R3 gate. Do not train.**

Then write a canonical interpretation without changing frozen definitions.

Only after that interpretation:

- if legal P/P+N evidence is sufficient: freeze the learner target, then run production-width **no-optimizer** GPU capacity preflight;
- if a reproducible residual warrants R4: freeze `R4_FORMULATION_PREREG` before R4 results;
- if legal representation remains insufficient: proceed to SOI-2/target-authority work;
- after representation closure + GPU preflight: freeze mini membership, style policy and observable checkpoint-selection key; write a new mini prereg;
- only then may an optimizer run be considered.

## Downstream closure remains open

Controlled frontend success is not product closure. E0-E5 remain:

`exact observable substrate -> Geppetto ceiling -> extractability -> predicted-vs-exact consequence -> ambiguity stress -> functionally equivalent 2.5D rigging substrate`.

## Research rule

`apparatus/data -> representation/target -> learner/optimizer -> evidence consumer -> downstream sufficiency -> only then information limit`
