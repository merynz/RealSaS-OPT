# RealSaS-OPT

Private canonical research workspace for RealSaS.

> **START HERE — active audit branch:** `audit/iris-architecture-discipline-20260824`
>
> **Current frontier:** V2 Representation Authority apparatus is closed and CI-verified. **ALL TRAINING IS FORBIDDEN.** The next executable is the real frozen 256-asset optimizer=0 R0-R3 measurement.
>
> In a new session, `GitHub'a bak ve devam et` means: read `CURRENT_STATE.md` first. Then inspect `experiments/iris_single_pose_v2/R0_R3_RUNBOOK_V1.md` and the audit only as needed. Do not execute historical V1 launchers.

## Current authority order

1. `CURRENT_STATE.md` — single continuation authority.
2. `experiments/iris_single_pose_v2/R0_R3_RUNBOOK_V1.md` — exact current optimizer-zero execution entry.
3. `experiments/post_corpus_audit/REPRESENTATION_AUTHORITY_STUDY_V1_PREREG.md` — parent scientific preregistration.
4. `experiments/post_corpus_audit/REPRESENTATION_AUTHORITY_STUDY_V1_EXECUTION_ADDENDUM_20260824.md` — frozen execution semantics.
5. `experiments/post_corpus_audit/REPRESENTATION_AUTHORITY_PANEL_LOCK_V1.json` — exact pre-result panel identity.
6. `canonical/PRODUCT_CONTRACT_V1.md` — product responsibility boundary.
7. `canonical/OBSERVABLE_RIGGING_SUBSTRATE_CONTRACT_V1.md` — canonical IRIS target.
8. `audit/IRIS_ARCHITECTURE_AUDIT_20260824.md` — reconciliation/closure record.
9. `experiments/iris_single_pose_v2/` — sole active implementation path.
10. `experiments/iris_controlled_v1/` — historical/diagnostic lineage; **DO NOT RUN**.

## Current code authority

Verified code-bearing commit:

`c1f80b5c4061a248da3af2f8080f4ebc4496d4b0`

GitHub Actions `IRIS V2 Preflight` run **#57**, ID `32671291295`: **SUCCESS**.

This covers the current Representation measurement, physical firewall/cache apparatus, semantic preflight, compact evidence handoff and final artifact SHA binding. It does **not** constitute a real-corpus result.

## Frozen product boundary

```text
ONE neutral pose x 8 ordered views
        ↓
IRIS observable geometry/persistence evidence
        ↓
deterministic SurfaceBuilder
        ↓
RiggingSurface
        ↓
Geppetto → skeleton/hierarchy
        ↓
Arachne → skinning
        ↓
Compiler → verified editable puppet
```

IRIS does not recover privileged hidden owner IDs or source-rig exactness. It must preserve the richest justified observable geometry and ambiguity needed by downstream rigging.

## Immediate execution

From `experiments/iris_single_pose_v2/` on the active branch:

```bash
python run_representation_authority_v1.py --mode all
```

This remains **optimizer=0** and keeps CAL/DEV/EXTERNAL sealed. The real result must be interpreted separately before any learner/training authorization.

## Audit discipline

No mini/scale/sealed learner run is permitted merely because code exists. Training may only be considered after:

- real R0-R3 measurement;
- canonical representation interpretation;
- any justified R4/SOI-2 work;
- production-width optimizer-zero GPU preflight;
- frozen mini membership, style policy and observable checkpoint-selection key;
- new preregistration.

Historical results remain provenance, but superseded executables may not regain active authority.
