# RealSaS-OPT

Private canonical research workspace for RealSaS.

> **START HERE — active research branch:** `g0-g1/single-pose-geometry`
>
> **Current canonical frontier:** the observable rigging-substrate problem is frozen; **S0-A information/derivability inventory is next**. The previously frozen G1 single-pose geometry baseline remains intact and has not started training.
>
> In a new ChatGPT session, “GitHub'a bak ve devam et” means: read `CURRENT_STATE.md` first, then the observable substrate contract, product contract, S0 plan, and preserved G1 freeze files below.

## Canonical continuation order

1. `CURRENT_STATE.md` — single continuation authority.
2. `canonical/OBSERVABLE_RIGGING_SUBSTRATE_CONTRACT_V1.md` — canonical scientific problem and head-derivation rule.
3. `canonical/PRODUCT_CONTRACT_V1.md` — IRIS → SurfaceBuilder → Geppetto → Arachne → Compiler responsibilities.
4. `experiments/s0_rigging_substrate/S0_PLAN_AND_PREREG_V1.md` — S0-A/B/C substrate sufficiency program.
5. `experiments/g0_g1_single_pose_geometry/G0_CONTRACT_FREEZE.json` — preserved frozen G0 baseline semantics/evaluator contract.
6. `experiments/g0_g1_single_pose_geometry/G1_SPLIT_FREEZE.json` — exact preserved G1 family/sample/truth-access freeze.
7. `experiments/g0_g1_single_pose_geometry/G1_PREREG_V1.json` — preserved frozen G1 training/evaluation contract.
8. `experiments/g0_g1_single_pose_geometry/G1_EXECUTABLE_PREFLIGHT.json` — live-corpus no-optimizer preflight witness.
9. `experiments/g0_g1_single_pose_geometry/G1_FREEZE_MANIFEST.json` — G1 source/hash authority.
10. `experiments/g0_g1_single_pose_geometry/PRESERVATION_LEDGER.json` — prior-work retention authority.

## Active product path

```text
ONE neutral pose × 8 ordered views
        ↓
      IRIS
        ↓
observation-grounded rigging-sufficient geometry
        ↓
 deterministic SurfaceBuilder
        ↓
 canonical RiggingSurface
        ↓
    Geppetto
        ↓
skeleton / hierarchy proposal
        ↓
     Arachne
        ↓
skinning / weight proposal
        ↓
     Compiler
        ↓
editable functional puppet
```

IRIS owns observation-grounded geometry, not authored mechanical identity. The final learned head list is determined by S0 downstream-sufficiency ablations. The current `P/N/V/U + persistence/provenance` set is the frozen G1 baseline candidate, not an assumed final product contract.

Geometric surface correspondence/persistence is **required**; an explicit learned `Z` descriptor is only one optional representation of that capability. High-recall correspondence, reciprocal/cycle consistency, camera-aware fusion, grounding and ambiguity preservation remain callable geometry tools.

## Preservation rule

No research component may silently disappear. Every lineage component is `KEEP_ACTIVE`, `RESERVE_CALLABLE`, or `DEPRECATED_WITH_PROOF`. Removal from the active forward path is not deletion.

The frozen G1 baseline remains historically intact: S0 is inserted as a more fundamental downstream-sufficiency program and does not rewrite G1 preregistration or architecture.

## Storage model

- **GitHub** — canonical source, preregistrations, tests, compact results, hashes and handoff state.
- **Google Drive** — heavy corpora, caches, checkpoints and proof packs.
- **ChatGPT Library** — transient diagnostics/working artifacts when useful.
