# RealSaS-OPT

Private canonical research workspace for RealSaS.

> **START HERE — active audit branch:** `audit/iris-architecture-discipline-20260824`
>
> **Current frontier:** execute the frozen **optimizer=0 R0-R3 Representation Authority gate** on the exact locked 256-asset OPEN panel. **ALL TRAINING IS FORBIDDEN.**
>
> In a new session, `GitHub'a bak ve devam et` means: read `CURRENT_STATE.md`, then `audit/IRIS_ARCHITECTURE_AUDIT_20260824.md`; continue from the unchecked item there. Do not execute historical Controlled V1/M256 launchers.

## Current authority order

1. `CURRENT_STATE.md` — single continuation authority.
2. `canonical/PRODUCT_CONTRACT_V1.md` — product responsibility boundary.
3. `canonical/OBSERVABLE_RIGGING_SUBSTRATE_CONTRACT_V1.md` — canonical IRIS scientific target.
4. `experiments/g0_g1_single_pose_geometry/FRONTEND_NATIVE_1024_CORPUS_CONTRACT_V1.md` — native-resolution/data authority.
5. `experiments/post_corpus_audit/IRIS_USABLE_CONTROLLED_CORPUS_FREEZE_V1.md` — 3930 controlled-corpus authority.
6. `experiments/post_corpus_audit/REPRESENTATION_AUTHORITY_STUDY_V1_PREREG.md` — parent exact/noisy representation prereg.
7. `experiments/post_corpus_audit/REPRESENTATION_AUTHORITY_STUDY_V1_EXECUTION_ADDENDUM_20260824.md` — frozen R0-R3 execution semantics.
8. `experiments/post_corpus_audit/REPRESENTATION_AUTHORITY_PANEL_LOCK_V1.json` — exact pre-result 256-panel identity lock.
9. `audit/IRIS_RESEARCH_TO_EXECUTABLE_ROLE_MATRIX_20260824.md` and `audit/IRIS_ARCHITECTURE_AUDIT_20260824.md` — reconciliation/closure ledger.
10. `experiments/iris_single_pose_v2/` — **only active implementation path**.
11. `experiments/iris_controlled_v1/` — historical/diagnostic lineage; `HISTORICAL_ONLY__DO_NOT_RUN.md` applies.

## Next executable

From `experiments/iris_single_pose_v2/`:

```bash
python run_representation_authority_v1.py --mode all
```

Runbook:

`experiments/iris_single_pose_v2/R0_R3_RUNBOOK_V1.md`

This executable performs only:

```text
frozen source-authority verification
 -> exact locked 256 seed
 -> geometry-only physical staging
 -> exact/dense V2 truth cache
 -> read-only stage/cache audit
 -> 38 R0-R3 representation measurements
 -> panel identity re-verification
 -> RUN_COMPLETE_V1.json
```

It must retain:

- `optimizer_steps = 0`;
- `training_authorized = false`;
- `sealed_splits_opened = false`.

It cannot decide P sufficiency, R4 necessity, SOI-2 or training authorization; those belong to the subsequent canonical interpretation.

## Frozen product boundary

```text
ONE neutral pose x 8 ordered views
        ↓
IRIS observable geometry/persistence evidence
        ↓
deterministic SurfaceBuilder
        ↓
RiggingSurface
        ↓
Geppetto → skeleton/hierarchy
        ↓
Arachne → skinning
        ↓
Compiler → verified editable puppet
```

IRIS does not recover privileged hidden owner IDs or source-rig exactness. It must preserve the richest justified observable geometry and ambiguity needed by downstream rigging.

## Audit discipline

No mini/scale/sealed optimizer run is permitted merely because code exists. A training path becomes executable only after representation authority closes and the repository contains one mutually consistent, preregistered set of architecture, resolution/coordinate, corpus/firewall, loss-role, evaluator, checkpoint-selection and mini-membership contracts.

All historical results remain provenance, but superseded code may not be mistaken for active authority.
