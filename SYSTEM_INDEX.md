# RealSaS Structural System Index

> Navigation only. `CURRENT_STATE.md` is repository-wide continuation authority.

## Current learned stack

| Subsystem | Current code/evidence home | State |
|---|---|---|
| IRIS V2 observation/foundation | `models/iris/v2/` | PROMOTED SUPPORT LAYER |
| historical IRIS V3 signed geometry | `models/iris/v3/` | HISTORICAL MAGE FIT1 LINEAGE |
| corrected H1 observable surface | active branch `experiments/mage_full_subject_reclosure_v1/` | **FIT2 STAGE-0 CLOSED PASS** |
| corrected GSA8192 | active branch corrected H1/GSA artifacts | **8171 NODES / 23656 RELATIONS / REAL 8-VIEW PASS** |
| Geppetto reference-strength V1 historical | `models/geppetto/reference_strength_v1/` | FIT1-FROZEN / HISTORICAL-SCOPED |
| Geppetto FIT2 | active repair branch runner/evidence | **FRESH REFIT RUNNING** |
| SkinFieldCodec | `models/skin_field_codec/v1/` | RETAINED REPRESENTATION/RESEARCH LINEAGE |
| Arachne V5 historical | `models/arachne/v5/` | FIT1-FROZEN / HISTORICAL-SCOPED |
| Arachne FIT2 | active repair branch after Geppetto | BLOCKED ON FRESH GEPPETTO |

## Current product composition

`8 real RGBA + exact cameras`

-> corrected H1 observable evidence

-> deterministic GSA/RiggingSurfaceIR S

-> fresh Geppetto FIT2 proposal

-> Compiler QualifiedSkeletonIR G

-> fresh Arachne FIT2 proposal

-> Compiler QualifiedSkinIR W

-> directional observation-domain CDT

-> strict FIT2 product mesh qualification

-> QualifiedEditableMeshIR M

-> exact QualifiedMeshSkinIR B

-> component/mechanical assembly

-> motion + dynamic proof

-> exact runtime/export.

Current active executable branch: `repair/mage-full-subject-reclosure-20260912`.

## Compiler mainline ownership

Canonical qualification/product ownership remains under `compiler/realsas_compiler_core/`.

Compiler owns deterministic:

- admitted GSA/RiggingSurfaceIR provenance;
- skeleton graph legality/canonical IDs;
- skin reference/simplex legality;
- directional product mesh support/coverage/topology qualification;
- exact mesh-skin transfer;
- component/mechanical assembly legality;
- proof and exact runtime/export identity contracts.

Learned proposal failures may be rejected, but Compiler may not silently synthesize missing learned semantics into a PASS.

## Directional mesh status

The real FIT1 end-to-end contradiction established mesh as a first-class product component rather than a debug/render helper.

Active-branch product qualifier:

`compiler/realsas_compiler_core/mesh/product_qualification.py::qualify_fit2_product_mwb2_observation_cdt_mesh(...)`

Current invariant:

- exact `ObservationRasterDomain` required;
- low-level legal CDT qualification is compatibility evidence only;
- exact promoted mesh raster positions are reconstructed from qualified surface support;
- exact promoted triangles are independently rerasterized;
- coverage and topology metrics are recomputed;
- candidate-reported residual coverage is diagnostic only;
- frozen product gates apply fail-closed;
- runtime/export must consume the exact qualified topology.

Mesh product contract/implementation: **CLOSED PASS**, self-hosted run `34716890157`, `41 passed in 4.36s`. Corrected real FIT2 mesh result: **PENDING FRESH G/W**.

## Supported inserted vertices

`LOCAL_CONVEX_INTERPOLATION` is the legal inserted-vertex route. Rest position, raster position and transferred skin must derive from the same admitted nonnegative simplex over S.

Automatic unsupported quality-Steiner insertion is not product authority.

## Runtime / product

Runtime and product surfaces consume proof/qualification-owned state; they do not mint learned semantics or topology.

Forbidden after qualification:

- hidden retriangulation;
- replacement alpha-clipped mesh;
- mechanics transfer onto a different topology;
- silently dropping qualified visible components.

Full `PRODUCT_PASS` remains a separate future acceptance contract after fresh Geppetto/Arachne, corrected real directional mesh, professional motion and exact runtime/export reclosure.

## Research/evidence zones

- `experiments/` — active/historical experiments, ablations and diagnostics;
- `canonical/` — decisions, preregistrations, seals, evidence and continuity state;
- `historical/` — provenance/source-diff reserve.

Historical FIT1 successes and A0/A1 failures are intentionally retained for scientific memory and later FIT8/LOFO design. Same-Mage FIT2 does not imply unseen-family generalization.
