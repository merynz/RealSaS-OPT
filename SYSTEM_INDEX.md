# RealSaS Structural System Index

> Navigation only. `CURRENT_STATE.md` is continuation authority.

## Learned stack

| Subsystem | Current mainline | State |
|---|---|---|
| IRIS V2 observation/foundation | `models/iris/v2/` | PROMOTED SUPPORT LAYER |
| IRIS V3 signed geometry | `models/iris/v3/` | PROMOTED MAGE FIT1 WITNESS |
| Geppetto reference-strength V1 | `models/geppetto/reference_strength_v1/` | FIT1-FROZEN / PROMOTED |
| SkinFieldCodec | `models/skin_field_codec/v1/` | RETAINED REPRESENTATION/RESEARCH LINEAGE |
| Arachne V5 | `models/arachne/v5/` | **FIT1-FROZEN / PROMOTED** |

## Current rigging-core composition

`8 raster observations + exact cameras -> IRIS -> deterministic GSA/RiggingSurfaceIR -> Geppetto -> Compiler QualifiedSkeletonIR -> Arachne V5 -> Compiler QualifiedSkinIR`

Arachne V5 checkpoint authority is composite and hash-bound; large model bytes live in Drive while Git stores exact source/identity contracts.

## Compiler mainline

Canonical qualification/product ownership remains under `compiler/realsas_compiler_core/`. Skeleton legality, canonical IDs, skin simplex/reference legality, mesh binding, proof/export and product composition are deterministic Compiler responsibilities.

## Runtime / product

Runtime and product surfaces consume proof/qualification-owned state; they do not mint learned semantics. Full PRODUCT_PASS is still a separate future acceptance contract.

## Research/evidence zones

- `experiments/` — active/historical experiments, ablations and diagnostics;
- `canonical/` — decisions, seals, evidence and continuity state;
- `historical/` — provenance/source-diff reserve.

Historical A0/A1 failures are intentionally retained for FIT8/LOFO scientific memory.
