# RealSaS — Arachne Information-Preservation Audit V1 — Status / Remaining Gates

**Date:** 2026-09-11  
**Status:** `OPEN__NOT_FINISHED__EVIDENCE_ONLY`  
**Branch:** `audit/arachne-information-preservation-v1-20260910`  
**Continuation authority:** `main/CURRENT_STATE.md`  
**Running A0 token-capacity experiment:** unchanged; this audit must not mutate its preregistration, optimizer, gates, winner rule, or result semantics.

This file is a progress index. It does not supersede the detailed phase records and does not authorize A1 implementation/training.

## Completed audit phases

### Phase 1 — end-to-end information-preservation map

Record: `canonical/ARACHNE_INFORMATION_PRESERVATION_AUDIT_V1_20260910.md`

Established:

- GSA -> promoted Geppetto is the positive-control boundary: rich typed surface evidence is preserved fieldwise rather than collapsed to the legacy summary view.
- Geppetto -> Compiler canonical graph authority is structurally correct, while selected evidence accessibility requires a separate semantic audit.
- Qualified `S + G` -> historical A1 consumer adapter is materially lossy and is P0 before V7-native A1 preregistration.
- A0 codec interface and A1 conditioning interface are separate objects and must not be conflated.

### Phase 2 — exact evidence census / Mage artifact recovery

Records:

- `canonical/ARACHNE_INFORMATION_PRESERVATION_AUDIT_V1_PHASE2_EVIDENCE_CENSUS_20260910.md`
- `canonical/ARACHNE_INFORMATION_PRESERVATION_AUDIT_V1_PHASE2_EVIDENCE_CENSUS_20260910.json`

Established:

- exact Mage dense zero-surface: 118,388 vertices / 234,962 faces;
- compact GSA: 950 nodes / 2,813 undirected relations;
- Geppetto `support_surface_ids` are sparse top-k mechanical anchors, not exhaustive skin ownership;
- exact Mage qualified skeleton has 176 joint->surface support references covering 155 unique compact surface nodes;
- `unsupported_joint_count == 0` is a joint-support-existence test, not a surface-coverage test;
- upper Mage hood/head teacher rows are rigid head-bound in the current teacher field;
- rigid attachment/assembly semantics remain a separate product-coverage item; current Mage staff/shield classification is not yet recovered.

### Phase 3 — semantic calibration audit

Records:

- `canonical/ARACHNE_INFORMATION_PRESERVATION_AUDIT_V1_PHASE3_SEMANTIC_CALIBRATION_20260910.md`
- `canonical/ARACHNE_INFORMATION_PRESERVATION_AUDIT_V1_PHASE3_SEMANTIC_CALIBRATION_20260910.json`

Established:

- IRIS V3 `log_uncertainty` is not calibrated uncertainty in the promoted Mage lineage; its current training lineage is a legacy auxiliary scalar with zero-centering regularization and no signed-stage optimizer update;
- therefore it should not be propagated downstream merely because a tensor exists;
- Geppetto mechanical salience magnitude is saturated/non-calibrated on FIT1 and low priority for A1;
- Geppetto position sigma has a real heteroscedastic training semantics but requires calibration/generalization evidence before downstream admission;
- raw parent/root alternatives remain Compiler-authority-sensitive;
- historical A1 compression is worse than nominal 20D/8D widths suggest because several retained fields are constant or duplicate on Mage.

### Phase 4 — holdout deformation signal connectivity

Record: `canonical/ARACHNE_INFORMATION_PRESERVATION_AUDIT_V1_PHASE4_HOLDOUT_DEFORMATION_SIGNAL_20260911.md`

Established:

- the running token-capacity experiment does not backpropagate holdout data or any direct LBS/deformation consequence;
- training optimizes per-joint scalar fields with BCEWithLogits + 0.1*MSE + Dice on 192 GSA950 + 192 Dense8K queries per joint;
- cross-joint normalization and LBS deformation are evaluation-only;
- therefore the user's suspicion is correct: a gradient that directly optimizes `hold_deform` does not reach the optimizer under the current preregistered objective;
- the evaluation metric itself is connected to model outputs and is not constant/disconnected;
- current synthetic translation probes are useful deterministic co-metrics but are not full articulated physical deformation proof;
- no running-experiment change is authorized. A later zero-optimizer forensic will localize whether the flat hold deformation comes from post-normalization joint balance, deformation-sensitive row structure, or probe insensitivity/cancellation.

## Audit is NOT complete

The audit remains open because the following gates are unresolved.

### Phase 5 — full field-by-field consumer-boundary matrix

Required output:

`RiggingSurfaceIR + QualifiedSkeletonIR -> legal information inventory -> historical A1 representation -> proposed V7-native A1 boundary candidate`

For every field/channel classify:

- semantic owner;
- product-time legality;
- teacher dependence;
- exact/derived/provenance/private status;
- preserved/compressed/dropped in historical A1;
- constant/duplicate status on Mage where measurable;
- whether the future boundary should preserve it;
- whether neural admission is baseline, ablation-only, or forbidden.

This is the primary remaining P0 architecture audit.

### Phase 6 — owner-clean qualified skeleton evidence sidecar design audit

No code implementation yet. Determine whether any selected-joint confidence state deserves typed downstream preservation without creating a second skeleton authority.

Candidates currently include position sigma / qualified structural margin. Mechanical salience and raw parent/root alternatives are not baseline candidates under current evidence.

### Phase 7 — rigid attachment / assembly coverage recovery

Recover the exact Mage staff/shield classification if possible and separate:

- deformable surface;
- rigid bone attachment;
- unrelated scene geometry;
- assembly-only root/socket semantics.

Then audit FIT-k/FIT8 corpus coverage so rigid-attachment capability cannot be silently untested.

### Phase 8 — post-A0 frozen-interface diagnostics

Blocked only on completion of the running 4/8/16/32 arms and A0 interface selection. Zero-optimizer diagnostics:

- token permutation/order semantics;
- single-token/prefix ablation;
- latent-set equivalence / matching requirement;
- regional row error: upper rigid-head, torso, limbs, cape/back-protruding regions where product-legal definitions exist;
- pre-normalization scalar error vs post-normalization joint-row error;
- per-row/per-joint LBS contribution;
- current synthetic probe vs alternative deterministic articulated probe bank;
- joint-permutation equivariance of consequence metrics.

## Closure conditions

The audit may be called complete only when:

1. Phase 5 field-by-field matrix is complete;
2. every P0/P1 information seam has one of: `PRESERVE_BOUNDARY`, `OWNER_CONSUMED`, `PROVEN_REDUNDANT`, `SEMANTICALLY_INVALID`, or explicit `DEFER_WITH_REASON`;
3. no teacher/source-only feature is admitted into product-time A1 conditioning;
4. no raw Compiler-consumed proposal evidence is allowed to compete with canonical skeleton authority;
5. A0 winner/interface-dependent questions in Phase 8 are resolved after the running experiment finishes;
6. a final audit closure report records exact source/artifact hashes and explicitly states what changes are recommended vs authorized;
7. only after that closure may a separate V7-native A1 design/preregistration transaction convert audit findings into architecture/training authority.

**Current verdict:** `AUDIT_OPEN__MEANINGFUL_FINDINGS_ALREADY_FOUND__DO_NOT_STOP_AT_PHASE4`.
