# RealSaS — Mage FIT1 real-E2E reopening / FIT2 refit context — 2026-09-12

**Status:** `ACTIVE_CONTINUATION_CONTEXT__FIT2_FRESH_PIPELINE_REFIT`

This document exists to preserve the causal reason the Mage line was reopened after the apparently successful FIT1 rigging-core closures. It is context/authority navigation, not a new scientific PASS.

## What actually happened

The controlled Mage FIT1 model/Compiler gates closed locally, and the exact FIT1 S/G/W authorities were then wired through the real eight-view product stack. The resulting end-to-end puppet was visibly below the expected product quality. That real output falsified the assumption that local rigging-core PASSes were sufficient evidence for an editable Spine-class product.

The response is **not** to continue from the old FIT1 product state. The current program is a same-Mage **FIT2 fresh pipeline refit/reclosure** from corrected upstream authority.

## Failure classes exposed by the real E2E run

### 1. Upstream subject/geometry scope was incomplete

The original Mage H1/IRIS geometry authority did not cover the same full rendered subject seen in all eight RGBA observations.

Exact-camera forensic measurements established:

- full deformation-supported source geometry: about `99.46%..99.68%` source-alpha recall;
- historical promoted signed zero-surface: about `62.24%..67.01%`;
- old conservative relation-clique MWB2 product mesh: about `18.18%..32.31%`.

Therefore the old Mage S/G/W lineage remains historical evidence but is not sufficient current product authority.

### 2. Mesh/product identity and coverage were not closed strongly enough

The first real E2E integration exposed that a topologically legal mesh can still leave most visible artwork without a deformation domain. A later exported-runtime bundle forensic audit also showed a separate identity drift: the rendered runtime used an alpha-clipped barycentrically transferred demo mesh rather than the exact mesh/substrate that had been locally reasoned about.

That bundle audit measured roughly `64%..72%` source-alpha recall with near-perfect precision: large visible regions were simply absent from the deformation mesh. This number is retained here as **forensic continuation context from the 2026-09-12 exported-bundle audit**; it is not being promoted as a new repo-native experiment result in this transaction.

The missing invariant was:

> The exact `QualifiedEditableMeshIR + QualifiedMeshSkinIR + QualifiedSkeletonIR` that receives product qualification must be the exact deformation/render authority projected into runtime; runtime may not silently re-triangulate, fill or transfer mechanics to a second mesh truth.

### 3. Motion was a mechanical probe, not professional animation

The current product motion/evaluator lane is `ROTATION_ONLY_CURRENT_PRESET_V1`. The historical deterministic preset is useful as a deformation/mechanical probe, but it is not an artist-quality idle/run system. The observed run/idle therefore cannot be used as evidence that RealSaS has closed professional motion quality.

Motion must become a first-class product subsystem. Current preferred direction is **proposal + deterministic Motion Compiler + proof**, with artist-authored canonical clips as the gold reference/preset library. This remains a product architecture proposal until separately frozen/implemented; it is not a claimed current model closure.

### 4. Proof scope was too local

Existing proof could establish legality, finite deformation, simplex/reference validity and bounded geometric behavior while still missing the perceptual/product question: does the exact exported puppet preserve the artist's visible subject and move credibly?

Future product proof therefore requires exact-runtime rest equivalence plus dynamic V0..V7 evidence, coverage, triangle inversion/stretch/attachment checks, and motion-specific quality/contact/phase checks before any `PRODUCT_PASS` badge.

## Current corrected FIT2 authority

The current authority is `canonical/MAGE_FIT2_PIPELINE_REFIT_AUTHORITY_V1.json` on branch `repair/mage-full-subject-reclosure-20260912`.

Upstream is already reclosed far enough to feed the fresh refit:

- H1 observable product-surface gate: **PASS**;
- H1 run id: `20260912T074348Z`;
- product-clipped zero-surface SHA-256: `56073e8b348b828350c812ac44982b823237196d5ec2f361241877e9ae301925`;
- corrected GSA: `8171` nodes / `23656` relations;
- observed/completed nodes: `7391 / 780`;
- GSA lineage: `65319061d802c640717010dddf0fd71a66ee6bd2fd31f6e614386f4d2584d5da`;
- GSA tensorization: `fe351362e195164805cebb0f63b74d1861ef123458b20ac56607016e00a6c67e`;
- real V0..V7 GSA evidence seal: **PASS**.

## Active stage — fresh Geppetto refit

Geppetto FIT2 is **fresh from scratch** on the corrected GSA8192 lineage.

Binding rules:

- historical Geppetto checkpoint SHA `b75f991564b64cfcec9b50b006544380ee482362a8439775bb505002349cbc30` is preserved but **must not be loaded**;
- max steps `16384`;
- check every `64` steps;
- terminal closure requires `48/48` consecutive terminal checks;
- evaluation seeds `[11, 23, 47, 89]`;
- no threshold relaxation;
- no Arachne start before Geppetto terminal PASS + required real eight-view evidence.

The CPU/real-input preflight is PASS. The actual optimizer run is external GPU work and is currently active per operator context; no Geppetto FIT2 PASS is claimed until its result artifacts exist.

## Mesh/component hardening completed while Geppetto runs

Two branch commits harden the product closure without changing the running Geppetto science:

- `9dd533eb5c48408faa92bcfb3469b7d32fd39368` — `Harden FIT2 mesh coverage and component closure`;
- `ebe7358dbdafbf65068dde91f3eba39b58b784b6` — `Run FIT2 mesh and component closure tests`.

The self-hosted reclosure contract workflow at `ebe7358...` completed successfully on `realsas-wsl-1660ti`.

Implemented/frozen policy now includes:

- per-view alpha recall / precision / IoU;
- largest uncovered connected-region gate;
- per-large-alpha-component recall;
- duplicate/degenerate/non-manifold mesh checks;
- raster min-angle / aspect-ratio checks;
- exact mechanical component verification against current S/G/W;
- fail-close visible-surface component accounting;
- legal `LOCAL_CONVEX_INTERPOLATION` mesh vertices with position and skin derived from the same admitted surface-support simplex.

This is infrastructure/contract PASS, **not** a corrected FIT2 mesh result. The corrected mesh cannot close until fresh Geppetto + fresh Arachne produce the exact current mechanics.

## Steiner / directional mesh rule

Inserted vertices are allowed when they are mechanically supported:

`P(v_new) = Σ a_i P(S_i)` and `W(v_new) = Σ a_i W(S_i)`, with `a_i >= 0` and `Σa_i = 1`.

The typed support/binding path is implemented and tested. Automatic quality-Steiner generation from arbitrary CDT interior points still requires an exact local support-construction / point-location mechanism before those points can become product authority.

The product target is not a hallucinated watertight 3D body. It is a **directional alpha-domain watertight deformation mesh** per V0..V7: every visible opaque artwork region must have an admitted triangulated deformation domain, while true transparent holes remain holes.

## Current mandatory stage order

`corrected H1 observable surface`

-> `sealed GSA8192 + real V0..V7 evidence`

-> **`fresh Geppetto FIT2`**

-> `new QualifiedSkeletonIR + real V0..V7 skeleton evidence`

-> **`fresh Arachne FIT2`**

-> `new QualifiedSkinIR + real V0..V7 skin/deformation evidence`

-> `directional alpha-domain MWB2/CDT + supported Steiner refinement`

-> `QualifiedEditableMeshIR + QualifiedMeshSkinIR`

-> `QualifiedComponentSetIR / QualifiedMechanicalAssemblyIR`

-> `real V0..V7 deformation proof`

-> `professional motion closure`

-> `exact runtime/export reclosure`

-> only then `PRODUCT_PASS` consideration.

## Claim boundary

Current claims remain deliberately narrow:

- historical FIT1 scientific/model evidence is preserved;
- the real FIT1 product output exposed genuine end-to-end gaps and is **not** a product-quality success;
- corrected H1 observable authority and GSA8192 stage-0 evidence are closed;
- Geppetto FIT2 fresh refit is active, not passed;
- Arachne FIT2 has not started/closed;
- corrected FIT2 mesh/component runtime closure has not passed;
- professional motion has not passed;
- unseen-family generalization has not passed;
- `PRODUCT_PASS = false`.
