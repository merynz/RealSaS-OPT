# RealSaS — Canonical IRIS → Geppetto → Arachne → Compiler Product Contract V1

**Updated:** 2026-08-31  
**Status:** `CANONICAL_PRODUCT_RESPONSIBILITY_CONTRACT__COMPILER_IN_SYSTEM_LOOP__DOWNSTREAM_OVERLAP_BOUNDARIES_BOUND`

## North star

`ONE NEUTRAL 8-VIEW CHARACTER SHEET -> EDITABLE, RIGGED, ANIMATABLE PUPPET`

Pose B is not a shipping dependency unless future controlled end-to-end evidence proves a material quality gain unavailable downstream.

## Canonical system boundary

```text
8 ordered neutral views + known cameras
  -> IRIS observation-grounded geometry evidence
  -> deterministic GeometricSubstrateAssembler / RiggingSurfaceIR
  -> Geppetto skeleton/hierarchy proposal G*
  -> Compiler hierarchy qualification -> QualifiedSkeletonIR G
  -> Arachne skin proposal W*
  -> Compiler skin qualification -> QualifiedSkinIR W
  -> explicitly typed editable-mesh / mesh-weight projection seam [MUST CLOSE BEFORE FINAL ARACHNE/PRODUCT SEAL]
  -> Compiler CanonicalPuppetGraph Y
  -> motion/deformation proof + bounded owner-routed repair/re-proof
  -> engine-neutral runtime package
```

IRIS, Geppetto and Arachne are learned responsibility boundaries. Compiler is canonical product authority; runtime is a non-owning projection.

## IRIS

Single responsibility: recover the **minimal observation-grounded geometric substrate sufficient for downstream rigging**.

Closed evidence currently narrows the required exposed learned geometry direction to forward depth `d` under known cameras; common-frame `P` is analytic (`P=O+dF`). N-B3 found no explicit learned normal feature required under its frozen tested regime. This does **not** claim predicted-depth product qualification; the structured ray-aligned tolerance bridge remains the next gate.

**Mode scalability invariant:** across known-camera Mode G and more uncertain Mode E, IRIS preserves the same analytic-geometry / learned-evidence topology; increasing observation or camera uncertainty may increase the relative reliance and calibration burden on learned correspondence/reliability evidence, but may not introduce hidden completion or a second geometry authority without explicit contract revision.

IRIS does not own authored mechanical owner identity, source-rig exactness, skeleton topology/parents, skin weights or product-canonical IDs.

## GeometricSubstrateAssembler

`SurfaceBuilder` is the historical/current implementation name; `GeometricSubstrateAssembler` is the planned functional nomenclature and will be applied only through an atomic compatibility migration.

This deterministic layer owns analytic point construction, provenance/support bookkeeping, admitted teacher-free persistence and other explicitly qualified deterministic geometric transforms. Local relations are geometric/morphological only. It is not a fourth learned model and may not silently absorb Geppetto/Arachne mechanics, canonical skeleton hierarchy, skin semantics or hidden-surface completion.

## Geppetto

`RiggingSurfaceIR S -> SkeletonProposalIR G*`.

Owns learned joint/control and hierarchy **proposal evidence** generation. Exact source-rig recovery is not required when a cleaner functionally equivalent rig exists. Proposal IDs/parents are not canonical authority.

## Compiler hierarchy qualification

Consumes `S + G*` through the restored canonical graph optimizer and materializes `QualifiedSkeletonIR G`. Owns root/parent validity, graph invariants, bounded structural policy, canonical ID minting and fail-closed admission.

Qualification may select among sufficiently supported Geppetto hypotheses; it may not silently return to generic model-free anatomy discovery from geometry when required joint/edge evidence is absent, except through a separately typed and preregistered fallback/completion proposal route.

## Arachne

`S + QualifiedSkeletonIR G -> SkinProposalIR W*`.

Owns learned **semantic influence-field / editable skin proposal** generation: which qualified controls influence which admitted geometry and with what proposed continuous field/weight evidence. Internal dense/sparse/tokenized representation is revisionable; the external contract must remain qualifiable and editable.

## Compiler skin qualification

Normal learned route:

`S + G + W* -> bounded mathematical/admissibility projection -> QualifiedSkinIR W`.

Compiler owns lineage/legal-reference checks, finite/nonnegative/simplex/influence policy, residual reporting and fail-closed admission. A numerical solver may impose a bounded mathematically admissible projection/regularization of Arachne evidence under a preregistered correction budget.

**Binding non-duplication rule:** Compiler skin qualification may not independently regenerate a materially different semantic skin solution and then call that Arachne's qualified output. A full BBW/QP/KKT solve that materially creates influence ownership or moves major mass across controls is a **new proposal producer**, not invisible qualification. If used as a deterministic fallback or comparison arm it must be separately typed/provenanced and pass through the same qualification/proof route.

## Editable mesh / mesh-weight seam

The current V1 typed library does not yet expose an explicit editable-mesh/discretization IR or an explicit qualified-skin-to-mesh-weight binding IR. Historical CDT/BBW/deformation machinery therefore must not be wired into the final product path implicitly.

Before final Arachne/product apparatus seal, a separate `MESH_WEIGHT_BINDING_CONTRACT` (exact final name/schema intentionally unsealed) must define:

- whether view-local/editable mesh is canonical product data or a deterministic projection of `RiggingSurfaceIR`;
- surface/raster/provenance lineage for every admitted mesh element;
- CDT/topology ownership and UNKNOWN/no-hidden-completion behavior;
- how Arachne's qualified field/weights are evaluated/interpolated/projected onto mesh vertices;
- residual/failure/coverage policy for that transfer;
- BBW/QP/KKT role in normal path versus explicitly separate fallback proposal path;
- how mesh/weight changes create a new product state and invalidate stale proof.

A triangulator is a discretization operator, not a second geometry truth authority. A mesh-weight projector is a deterministic projection of admitted semantic weight evidence, not a second Arachne.

## Compiler product authority

Compiler owns product assembly, the single `CanonicalPuppetGraph` lineage, exact-state deformation/contact execution, motion/deformation proof, attribution, bounded owner-routed repair/reselection, mandatory re-proof and export.

Repair is not a hidden monolithic auto-rigger. A failure must be attributed to an owner domain; any accepted rest geometry/skeleton/skin/mesh mutation creates a new `Y'` and invalidates stale proof.

Proof, repair, serialization and runtime must bind to the exact same `CanonicalPuppetGraph.product_state_hash`. Model-issued product truth and parallel canonical graphs are forbidden.

## Numerical downstream operators

CDT, BBW/QP/KKT, ARAP and XPBD/contact remain valuable Compiler numerical machinery, but their authority classes differ:

- CDT: derived/editable discretization of admitted geometry, never hidden-surface truth;
- BBW/QP/KKT: bounded skin projection/regularization in the normal Arachne path, or an explicitly separate deterministic skin-proposal arm if used for full synthesis;
- ARAP: deformation solve on exact admitted product state;
- XPBD/contact: constraint/contact solve on exact admitted product state.

ARAP/XPBD may emit residual/failure evidence but may not silently rewrite canonical rest geometry, skeleton or skin.

## Quality contract

IRIS promotion requires geometry/downstream evidence, not teacher-rig exactness. Geppetto/Arachne promotion requires editable functional quality plus compiler qualification. Final product promotion requires deformation/motion proof and fail-closed behavior.

Current E0 and N-B3 results are scientific information-sufficiency/feature-necessity results, **not** a product pass. The active IRIS program is now the reprojection-centered V2 gate sequence under its newer canonical preregistrations.

The downstream ownership audit authority is `DETERMINISTIC_DOWNSTREAM_LAYER_OWNERSHIP_OVERLAP_AUDIT_20260831.md`.

## Change control

Boundary changes require recorded controlled evidence. Convenience, analogy or implementation fashion are insufficient. Explicit revision is required before returning hidden mechanical ontology to IRIS, making Pose B mandatory, allowing model-owned canonical truth, allowing a Compiler numerical solver to become a hidden second learned-semantic owner, or replacing functional product gates with source-teacher exactness.
