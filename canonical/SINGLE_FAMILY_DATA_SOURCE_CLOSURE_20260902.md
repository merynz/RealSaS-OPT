# Single-Family Data Source Closure V1

Status: SOURCE_IMPLEMENTED__FIT_NOT_AUTHORIZED

This batch implements DATA-01/02 from the canonical completion matrix:

- exact one-family Master byte manifest over primary geometry plus 8 explicit RGBA/camera/raster bindings;
- image authority is an explicit filename and is never silently chosen;
- 1024 RGBA, camera order/contract and raster authority are fail-closed;
- optional expected file/manifest hashes detect dirty bytes;
- training-only dense mechanical skin truth rebinds to current RiggingSurfaceIR and Compiler-qualified QualifiedSkeletonIR.v2;
- control alignment uses only QualifiedJoint.source_proposal_id against anonymous teacher control IDs;
- no source bone names/indices/tails are accepted or emitted in product-facing tensors;
- truth lineage binds exact S and qualified-G hashes.

Synthetic source gates authorize no family selection, fitting, checkpoint selection or generalization claim.
