# Compiler Services

Subordinate execution services for the current typed Compiler.

Planned service groups during restoration:

- `proof/` — motion/deformation probe execution and causal measurements;
- `repair/` — failure signatures, owner attribution, bounded repair directives and re-proof loop;
- `export/` — proof-gated `.rss/.rsr` projection;
- `numerics/` — only source-diff-selected CDT/BBW-KKT/ARAP/XPBD kernels.

No module in this tree may mint canonical joint/part identity or bypass `realsas_compiler_core`.
