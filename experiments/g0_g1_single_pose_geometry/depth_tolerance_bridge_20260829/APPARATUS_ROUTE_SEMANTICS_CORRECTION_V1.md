# RealSaS — Structured Depth Bridge Route Semantics Correction V1

**Date:** 2026-08-29  
**Status:** `PASS__LABEL_OVERLOAD_CORRECTED__M0_M3_EVIDENCE_PRESERVED__M4_USES_TRUE_D2_ROUTE`

## Why this correction exists

The M2/M3 apparatus closure used the name `MUTUAL_P003` for the carrier emitted directly by the
teacher-free `derived_match_row` stage. Historical E0 D2 semantics are stricter than that label implied.

The exact recovered E0 downstream authority proves the real route is:

```text
corrupted observable P
 -> BASE derived_match_row persistence
 -> reciprocal reverse match
 -> admit iff cycle_P <= 0.003
 -> historical D2 carrier / X36
```

Therefore the M2/M3 `1909 / 1921 / 1910 / 1896` support-pair observations are **pre-cycle BASE
persistence apparatus evidence**, not post-cycle historical D2 support counts.

No M2/M3 numerical result is rewritten or discarded. Their role remains valid for:
- ray-aligned corruption execution;
- re-derived-normal execution;
- BASE teacher-free persistence execution;
- typed Compiler surface routing;
- independent/coherent and symmetric/asymmetric intervention smoke.

They may not be cited as evidence that historical D2 reciprocal admission itself retained those exact
support counts.

## Corrected canonical naming

Use these names from M4 onward:

- `BASE_DERIVED_MATCHER`: direct `derived_match_row` accepted pairs.
- `D2_MUTUAL_P003`: `BASE_DERIVED_MATCHER` plus an observation-only reverse match with
  `cycle_P <= 0.003`.

The current scientific surface/substrate grid MUST route `D2_MUTUAL_P003`, not the pre-cycle BASE
carrier, into both:
- the sealed D2 frozen proxy replay where that evaluator is legally applicable; and
- the typed Compiler surface used for grid qualification.

BASE remains recorded as a mechanism diagnostic so forward-match loss and reciprocal-cycle loss stay
separable.

## Zero-corruption parity after correction

Already-open witness:

`asset_551ea351b43a1787d0f55536`

The additive grid runtime reproduces both layers separately:

- BASE bridge support pairs: `1909`;
- sealed clean E0-B BASE support pairs: `1909`;
- BASE P/support/matched/source-view/source-row: bit-exact;
- historical D2 bridge support pairs after reciprocal cycle admission: `1884`;
- historical compact-pack D2 support pairs: `1884`;
- rebuilt historical `D2_X`: bit-exact.

Thus the correction changes route naming/selection, not the sealed observable geometry, corruption
operator, anchor sampler, or historical D2 evaluator semantics.

## Additive batch/cached scientific-grid runtime

The sealed M0-M3 apparatus sources are left unchanged. M4 receives an additive execution layer:

`bridge_grid_runtime_v1.py`

It:
- prepares clean observable authority and the P-only source-row sampler once per asset;
- reuses that immutable prepared state across corruption cells;
- vectorizes the scalar `derived_match_row` contract over anchors while preserving candidate ordering,
  gates, score formula and first-argmin tie semantics;
- computes the pre-cycle BASE layer explicitly;
- applies the historical observation-only reciprocal `cycle_P <= 0.003` admission explicitly;
- shares one corrupted observable state between BASE and D2 processing.

Real-witness parity:
- nonzero stress check: `epsilon=0.003`, `ell_px=16`, `ONE_BAD_HASHED`;
- tiny scalar comparison anchors: `8`;
- scalar forward row comparisons: `56`;
- vectorized rows versus scalar `derived_match_row`: exact;
- epsilon-zero historical D2 `D2_X`: exact.

Runtime measurements in the machine-readable result are diagnostics only and are forbidden from any
scientific tolerance decision.

## Bound additive artifacts

SHA-256:
- `bridge_grid_runtime_v1.py`: `4ebf3636438aa14b5f832d51da3e9222086b948b7480164dcd3c50d3ad4ca9dd`
- `run_batch_cached_route_parity_v1.py`: `3ec3b7da008f6568d03f41df907b206ccf9d3b75689a1f8ca11664d33d053da7`
- `BATCH_CACHED_ROUTE_PARITY_V1.json`: `eb94ecf0a31c5df0a772cf4e9eb3c4afc83db42a34335f2463c5f219b92b7ebe`

Bound witness inputs:
- sealed clean E0-B BASE carrier: `b23a769d60d32064222ea04cb8fe9b4b8d458a5d400d42666332f8ce84a5457a`
- historical D2 compact pack: `a4b3d5544f20a0fecefa877b74f4ad601f072d29afd0df35204c7b182d800c2c`

The existing frozen D2 proxy restoration remains authoritative for checkpoint/evaluator semantics; this
patch does not modify its source or checkpoints.

## Firewalls

- scientific tolerance outcomes opened: `false`;
- Proxy27: `CLOSED`;
- DEV32: `CLOSED`;
- training steps: `0`;
- M3 smoke values remain forbidden from selecting M4 grid cells or thresholds;
- frozen E0/N-B3 result bytes are unchanged;
- sealed M0-M3 result bytes are unchanged;
- this optimization is execution-only and must pass parity before any M4 result is admissible.

## Next

Freeze the M4 scientific cell list, report schema and decision operator on `calibration_anchor8` before
opening any grid outcome. M4 must explicitly distinguish BASE persistence diagnostics from
`D2_MUTUAL_P003` qualification.
