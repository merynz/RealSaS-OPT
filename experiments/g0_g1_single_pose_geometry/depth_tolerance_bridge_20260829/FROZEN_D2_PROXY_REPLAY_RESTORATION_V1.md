# RealSaS — Frozen D2 Proxy Replay Restoration V1

**Date:** 2026-08-29  
**Status:** `PASS__EXACT_SEMANTICS_RESTORED_FROM_EMBEDDED_NOTEBOOK_AUTHORITY__NO_TRAINING`

## Purpose

The structured predicted-depth bridge must evaluate the same frozen E0 D2
information-isolation proxy consumers that supported the sealed E0 decision.

The current GitHub tree retained the one-shot Proxy27 runner and checkpoint/result
seals, but not the imported historical helper sources
`e0_downstream_proxy_v1.py` and `run_e0_downstream_proxy_calibration_v1.py`.

No approximate reimplementation is permitted for scientific bridge outcomes.

## Recovered authority

The user's persistent Library contains:

`RealSaS_E0_DOWNSTREAM_PROXY_V1_2.ipynb`

The notebook embeds the historical source text and verifies it by SHA-256 before
execution.

Notebook authority SHA-256 recorded by the existing GitHub preflight:

`a46fe75a7ce5a0318d79c6d8b055bc593bd8c57942edc85c1d93353bf742047f`

Recovered embedded source SHA-256:

- `e0_downstream_proxy_v1.py`
  `0f599e8f717d4c5070c04e224d90e52d1dc6e76a6e2068e64ed7c9d9d2b950b4`
- `run_e0_downstream_proxy_calibration_v1.py`
  `7fc3021726df64069ba44be42f946db25929372497f39f392898477b374e2ca4`

These exactly match the pre-training `E0_DOWNSTREAM_PROXY_CONTRACT_V1.json`
source authority.

`proxy_replay_v1.py` copies only the no-training inference/evaluation semantics
needed for bridge replay and binds the sealed D2 checkpoint hashes.

## D2 admission parity

Historical D2 is not raw E0-B base persistence.

After base teacher-free persistence it applies the frozen observation-only
reciprocal cycle admission:

`reverse_match_exists AND cycle_P <= 0.003`.

The bridge therefore re-runs that exact admission after depth corruption.

On already-open calibration family:

`asset_551ea351b43a1787d0f55536`

at epsilon zero:

- historical D2 support pairs: `1884`;
- bridge-rebuilt D2 support pairs: `1884`;
- rebuilt `D2_X` versus historical compact pack: **bit-exact**;
- max absolute feature difference: `0`.

Thus the proxy replay adapter has an identity point at zero corruption.

## Evaluator parity

Sealed checkpoints:

- Geppetto D2:
  `f8c6146fc3ad81146ced01805b9be454ad194b86db3a9ab3d72d7b9eb3747b65`
- Arachne D2:
  `72898a62f23c55aa82047f7bc4b39be787abb97d14f9a3fb973d59b2b5689745`

The exact restored evaluator was replayed on all four truth-capable historical
calibration families using their sealed compact packs.

Compared with the historical calibration result:

- Geppetto maximum absolute numerical difference: `< 9e-8`;
- Arachne maximum absolute numerical difference: `< 1.2e-7`;
- discrete PCK/top1 and MAE/displacement values reproduce exactly where expected.

A fixed parity tolerance of `2e-7` is therefore sealed for CPU replay versus the
historical execution device.

## Interpretation

These models remain:

`INFORMATION_SUFFICIENCY_PROXY_NOT_PRODUCT_GEPPETTO_ARACHNE`.

They may be used by the current surface/substrate bridge with no retraining.

They do not satisfy the later consumer-validity interlock and cannot by themselves
define final product-safe IRIS tolerance.

## Firewalls

- scientific optimizer steps: `0`;
- Proxy27 bridge outcomes not opened for this restoration;
- DEV32 closed;
- M3 smoke outcomes not used to choose parity rules or tolerances.
