# RealSaS — Controlled DINOv2 Ladder Preregistration Amendment V1.2

**Date:** 2026-08-29  
**Status:** `BINDING_BEFORE_DINO_CANDIDATE_OUTPUTS__ZERO_OPTIMIZER_STEPS`

This amendment changes no candidate, metric, optimizer budget, PASS threshold or model architecture. It resolves a provenance/publication detail discovered during zero-step preflight.

## 1. Historical membership identity

The scientific requirement is **exact family-selection identity**, not byte-for-byte recreation of the historical JSON serializer.

The historical 512-family selection is accepted as recovered only when all of the following hold:

1. `MASTER_VARIANTS.jsonl` byte SHA-256 equals `475b12c6876a7ba91d8b1b32b6acac29536431134b44a96277a74c2493e86913`;
2. `FIT_PROXY32` is regenerated by per-source `sha256("PV5_R256_FIT_SCALE_PROXY32_V1|<asset_id>")` queues with frozen quota `26/3/3`;
3. those proxy families are excluded before training selection;
4. the training queues use `sha256("PV5_R256_FIT_SCALE_TRAIN_V1|<asset_id>")` with frozen 512 quota `483/24/5`;
5. recovered proxy and nested train-32 sets match the persisted historical evaluation authorities;
6. the recovered train-512 asset-ID set SHA-256 equals `1958fa5ed80430ac8ae8f9e66f8d94bc5bfe89c74b5553d13b891c87fdefb2a2`.

The old combined-membership JSON SHA `8531360f1c61dc4cdb699c095790c35ab3af0da5a7a290b420e5777bb4e9169b` remains historical provenance. Failure to reproduce its whitespace/key-order serialization is not a scientific population mismatch once the selection-equivalence proof above passes.

## 2. Native-1024 eligibility discovery

The exact recovered historical train-512 intersects the later Controlled-V1 training-eligibility freeze as:

- 503 immediately eligible;
- 9 Stage-B6 `repair_pending`;
- 0 active non-Basis shape-key quarantine;
- 0 all-eight-views blank quarantine.

The nine repair-pending members are:

- `asset_1d6d3b17fe506463b8840c51`
- `asset_4970fb8cc7c69df071970dc0`
- `asset_66c8c63c7a97e830b89d0ba8`
- `asset_674fb6e5571ca86dd0e5c858`
- `asset_681fb76277c10d2307f7509f`
- `asset_88fe0971a994597f3866473b`
- `asset_a873bb17b9a66e6e980845a5`
- `asset_ce3577373d7cc71b7025cc74`
- `asset_f56aff9ecf13f7ddf0afcd55`

All nine have already-frozen Stage-B6 normalized geometry artifacts. Their old A-pass render must not be paired with repaired geometry.

## 3. Binding repair/publication rule

Before DINO feature extraction or optimizer step 1:

1. publish the authorized frozen Stage-B6 normalized geometry for these exact nine assets through the selective Stage-B7 publication/rerender path;
2. regenerate the native observation products under the same canonical 1024×1024, 8-view, `cel_clean` / `ink_cel` contract;
3. verify geometry/render/raster-authority lineage for all nine;
4. re-run train-512 availability and require **512/512** PASS.

Forbidden:

- replacing any of the nine with another family;
- dropping them and training on 503;
- training against stale pre-repair renders;
- changing the 512 membership after seeing DINO candidate outputs.

## 4. Everything else remains unchanged

V1 and V1.1 remain binding for:

- frozen S/B/L/g candidates;
- shared architecture and parameter-count equality;
- 7168 diagnostic and 32768 primary checkpoints;
- direct actual-residual replay;
- FIT_PROXY32 >=61/64 PASS;
- TRAIN_DIAG32 >=61/64 PASS;
- zero hard-route failures;
- no DEV/CAL/external access;
- no candidate-specific tuning.

Scientific optimizer steps at this amendment: **0**.
