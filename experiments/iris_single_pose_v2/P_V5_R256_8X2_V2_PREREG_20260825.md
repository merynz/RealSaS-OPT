# RealSaS IRIS P-V5 R256 — Fresh 8×2 Certification V2 Preregistration

**Status:** `FROZEN__NOT_YET_RUN`  
**Scientific role:** fresh shared multi-asset certification after V1 failure localization  
**Unseen-family:** CLOSED  
**Architecture changes:** FORBIDDEN  
**PatchMatch:** FORBIDDEN

## Causal basis

This gate is not an inspection-driven retry of V1. It is authorized by three already-completed observations:

1. The original fresh 8×2 V1 is an immutable FAIL: 8/16 cells passed, worst P95 `0.008320469176396726`.
2. A preregistered +2048 low-LR continuation is also an immutable FAIL, but improves all 16/16 cells simultaneously and ends at 13/16 PASS, worst P95 `0.005740759451873588`, with the trajectory still descending.
3. The dominant hard asset `asset_36fb02305846592b1ecdf3d4` independently fits both frozen styles from a fresh model to about `0.00260` P95, proving that its shared-run failure is not intrinsic non-extractability under the current R256 representation.

Therefore the next falsifiable question is:

> Can the unchanged shared R256 learner certify the same frozen 8×2 membership when given a prospectively adequate, continuous low-LR tail from a fresh model?

## Frozen membership and data

Exactly the same `P_V5_R256_8X2_MEMBERSHIP_V1.json` membership as V1:
- 8 frozen FIT assets;
- `cel_clean` + `ink_cel` for every asset;
- 16 cells total;
- 8 views/cell;
- exactly 4096 seeded visible raster rows/view;
- style-paired cells share the exact same truth loci;
- no post-result membership selection.

No TUNE/CAL/DEV/sealed-family data may be opened.

## Frozen model / objective

- fresh `IRISSinglePoseV2PV5R256` initialization;
- no checkpoint reuse;
- same trainable P path as V1;
- N/U/coarse/fine heads frozen;
- same known-yaw observable P construction;
- same P-only SmoothL1 camera-forward depth objective;
- no augmentation;
- no camera JSON / hidden teacher camera;
- GroupNorm / zero effective dropout behavior unchanged.

Every optimizer update represents all 16 cells through two equal microbatches:

```text
8 cells × 8 views
  +
8 cells × 8 views
  ↓ divide each loss by 2
ONE optimizer update representing all 16 cells
```

## Frozen schedule

### MAIN
- AdamW
- LR `3e-4`
- betas `(0.9, 0.95)`
- weight decay `0`
- `2048` optimizer steps
- eval: `512, 1024, 2048`

### TAIL
- initialize from MAIN-2048 model weights;
- **fresh AdamW moments once at TAIL start**;
- LR `3e-5`;
- betas `(0.9, 0.95)`;
- weight decay `0`;
- **4096 continuous optimizer steps**;
- eval: `256, 512, 1024, 1536, 2048, 2560, 3072, 3584, 4096`.

The full schedule runs even if an earlier evaluation checkpoint passes. No early stop is allowed.

Total planned optimizer steps: **6144**.

## Checkpoint authority improvement

Every preregistered V2 checkpoint must contain:
- model state;
- optimizer state;
- GradScaler state;
- Python / NumPy / Torch CPU / Torch CUDA RNG states;
- phase + phase step + total optimizer step metadata.

This removes the ambiguity that prevented an exact optimizer-state continuation from the original V1 checkpoint.

## PASS / FAIL

At every preregistered evaluation checkpoint, compute canonical P Euclidean error for each of the 16 cells independently.

**PASS** iff at least one preregistered checkpoint has:

```text
16 / 16 cells with P_p95 <= 0.005
```

The selected checkpoint is the preregistered candidate with lexicographically lowest:
1. worst-cell P95;
2. aggregate P95;
3. total optimizer step.

Aggregate P95 alone cannot pass.

### If PASS

- declare `P_V5_R256_8X2_V2_CERTIFICATION_PASS`;
- do not rewrite either prior FAIL;
- unseen-family still remains unopened until its own preregistration is frozen;
- next scientific gate: unseen-family generalization.

### If FAIL

- declare `P_V5_R256_8X2_V2_CERTIFICATION_INSUFFICIENT`;
- remain at 8×2;
- true shared capacity/interference becomes a materially stronger hypothesis;
- localize before any architecture expansion or PatchMatch intervention.

## Firewalls

- Original 8×2 V1 FAIL remains immutable.
- +2048 continuation FAIL remains immutable.
- 36fb solo PASS is causal localization evidence, not a replacement certification.
- No PatchMatch.
- No architecture change.
- No style ID.
- No new assets.
- No unseen family.
- No result-driven schedule extension after this preregistration.
