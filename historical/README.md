# Historical Authority Index

This directory is **not** a second source tree. It records provenance and promotion decisions for source that remains in external archives or historical Git lineage.

Current restoration authority is recorded in `canonical/COMPILER_RUNTIME_PROMOTION_MANIFEST_V1_20260903.json`.

The exact full v0.5 archive remains external byte authority; only explicitly promoted, regression-gated mechanisms are copied into current executable paths.
