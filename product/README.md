# RealSaS product surfaces

`product/` contains current user-facing application surfaces. Product code is a **consumer** of Compiler authority; it may not mint canonical product truth.

- `living_compile/` — current V4 Living Compile inspector, runtime preview, and optional puppet authoring editor.

User edits are always stored as non-authoritative authoring layers and must return through Compiler qualification + dynamic proof before deployment.
