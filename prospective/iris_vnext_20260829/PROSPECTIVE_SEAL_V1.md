# RealSaS IRIS V-next — Prospective Design Seal V1

**Seal date:** 2026-08-29  
**Status:** `PROSPECTIVE_NONCANONICAL_UNOPENED__DO_NOT_OVERRIDE_CURRENT_STATE`  
**Parent canonical main:** `6689393f66f3fd6cefff087a1902e2a7ac3a8061`

This directory is a sealed snapshot of the architecture/preregistration design discussion that followed N-B3.

It is deliberately **not** a new canonical scientific state and does not open any experiment.
`CURRENT_STATE.md` remains the sole continuation authority. The current executable NEXT remains the
structured ray-aligned predicted-depth tolerance bridge.

## Sealed files

```json
{
  "IRIS_VNEXT_ARCHITECTURE_V6.svg": {
    "bytes": 9858,
    "sha256": "21b9b40f2e91702ea905fb394dadfa42d6ea1700e4e5b40c112942eebc059d36"
  },
  "IRIS_VNEXT_EXPERIMENT_PLAN_V4.md": {
    "bytes": 10694,
    "sha256": "9cefbb8308f2c0984297c1cbd2c2e83a6510bb5ff319174f335f091dccbc7e6d"
  },
  "OBSERVATION_CONTRACT_V1.md": {
    "bytes": 9647,
    "sha256": "42e11a7e0e1f687771c3a12048ffd4174d13aa428d5ef837c30045653ef2727f"
  }
}
```

## What this snapshot preserves

- depth `d` as the single learned geometric authority;
- analytic `P = O + dF` under exact orthographic cameras;
- `N_aux` as training-only, with product normals derived deterministically from `P`;
- alpha/support as observation authority;
- task-agnostic geometric risk only;
- directed cross-view reprojection + separate silhouette exclusion;
- immutable model-independent `VisualHullEnvelope`;
- conservative `RefinedRiggingVolume`;
- Geppetto proposal -> Compiler qualification -> Arachne proposal -> Compiler qualification;
- future controlled DINOv2 capacity ladder and separate full-system MapAnything/DA3 comparison;
- versioned ObservationContract concept for exact-camera guaranteed input vs future external best-effort input.

## Known prospective amendments intentionally NOT reopened here

The final review raised four future refinements that are recorded but not allowed to delay the current bridge:

1. replace a spatially constant prospective common risk band with a candidate-independent observable-conditioned band and check non-degeneracy on sacrificial data;
2. calibrate future external cross-view inconsistency using both consistent and deliberately contradictory observation sets;
3. measure a non-binding camera-perturbation sensitivity chart for a future ObservationContract version;
4. enforce a minimum-path execution gate before opening the larger prospective apparatus.

These are **future design amendments**, not requirements for the current `main` NEXT.

## Promotion rule

Nothing in this directory becomes canonical merely by existing on `main`.
Promotion requires a later explicit canonical decision after the structured predicted-depth bridge
(or another explicitly authorized gate) supplies the necessary evidence.

This seal is immutable by convention. Future changes supersede it with a new version rather than editing its scientific meaning.
